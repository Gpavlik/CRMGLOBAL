name: Deploy to MiroHost
on:
  push:
    branches: [ "main" ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.0
        with:
          server: vs2877.mirohost.net
          username: pharmvisit
          password: W3mHqpJJ3h48
          port: 21
          local-dir: ./
          server-dir: /var/www/pharmvisit/pharmvisit.net.ua/
          dangerous-clean-slate: true
